{
  "name": "webapp",
  "version": "1.0.0",
  "dependencies": {
    "parcel-bundler": "1.12.4"
  },
  "scripts": {
    "build:index": "parcel build Index.html",
    "build:sales": "parcel build Sales.html",
    "build:service": "parcel build Service.html",
    "build:lead": "parcel build Lead.html",
    "build": "npm run build:index && npm run build:lead && npm run build:service && npm run build:sales"
  }
}
